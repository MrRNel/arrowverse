# Copy to .env.docker for: docker compose up --build
# MySQL is external — allow connections from this machine / Coolify server IP.

APP_URL=http://localhost:8000
HOST_PORT=8000
PORT=8000

DB_HOST=169.239.218.66
DB_PORT=3306
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

JWT_SECRET=generate-with-openssl-rand-hex-32
CORS_ORIGINS=http://localhost:8000
PUBLIC_REGISTRATION=true
